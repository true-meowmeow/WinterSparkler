package obsolete.core.main.config;

final class ConfigFiles {
    static final String DEFAULTS = "/properties/app.defaults.properties";
    static final String USER_OVERRIDES = "/properties/app.user.properties";

    private ConfigFiles() {
    }
}
